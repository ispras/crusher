
# Фаззинг сервера nginx в докер-режиме с помощью Crusher

Рассмотрим фаззинг сервера `nginx`.

Докер-режим позволяет избежать целый ряд конфликтов за ресурсы (например, файлы и порты) между разными процессами исследуемого приложения.
Подробнее - см. раздел "Фаззинг в докер-контейнерах" в Руководстве пользователя (crusher/README.pdf).

## 1. Подготовка окружения запуска

Установка зависимостей (пример для Ubuntu 24.04):
```shell
$ sudo apt update -y
$ sudo apt install -y docker.io docker-buildx
$ sudo usermod -aG docker <user>
```

## 2. Создание докер-образа с исследуемым ПО

Создать докер-образ с исследуемым ПО и настроенным для него окружением:

```shell
$ ./docker/docker_build.sh <crusher_dir> <HASP_IP>
```
где `<HASP_IP>` - IP сервера лицензий.

## 3. Запуск фаззинга

В поддиректории `in` находятся входные данные для фаззинга.

Пример запуска фаззинга:
```shell
$ <crusher_dir>/bin_x86-64/fuzz_manager --start 4 --eat-cores 2 --dse-cores 0 \
                             --docker nginx-fuzz \
                             --config-file config.json \
                             -i in -o out \
                             -- /root/fuzz/nginx-fuzz/sbin/nginx
```

Опции:

* `--start <num>` - число экземпляров фаззинга (fuzz-процессов);
* `--eat-cores <num>` - число процессов доп. анализа (EAT - Extra Analysis Tool);
* `--dse-cores <num>` - число процессов динамического символьного выполнения (DSE);
* `--docker <name>` - докер-образ с подготовленным окружением для nginx;
* `--config-file <path>` - конфигурационный файл;
* `-i <path>` - путь до директории с начальными входными данными;
* `-o <path>` - путь до директории с результатами фаззинга;

опции в конфиг-файле:

* `-T <type>` - тип транспорта (способ передачи данных в исследуемое приложение);
* `--ip X.X.X.X` - IP-адрес машины, на которой запускается целевая программа (т.е. localhost);
* `--port <num>` - порт, который слушает целевая программа;
* `--delay <msecs>` - в течение `<msecs>` миллисекунд фаззер пытается установить tcp-соединение с целевой программой (ожидание готовности целевой программы);
* `-I <type>` - тип инструментации;
* `--bitmap-size <num>` - размер битовой карты покрытия в байтах;
* `--clean-binary <path>` - путь до бинаря без инструментации для получения, например, lighthouse- и valgrind-отчётов;
* `--coverage-binary <path>` - путь до проинструментированного бинаря для получения отчёта о покрытии исходного кода (LCOV);
* ` --wait-next-instance <msecs>` - временной интервал (в миллисекундах) между запусками процессов fuzz, eat и др.;
* `-t <msecs>` - таймаут в миллисекундах (если приложение не отработало за указанное время, то результат запуска классифицируется как зависание);
* `-- <target command>` - команда запуска целевого приложения (в докер-режиме - пути указываются внутри докер-контейнера).

Для быстрого запуска демо-теста используйте скрипт `fuzz.sh`. Данный скрипт принимает на вход 1 аргумент - путь до директории `crusher/`.

Для более детального ознакомления с данным режимом - см. раздел `Фаззинг в docker-контейнерах` в Рук-ве пользователя. 
Для ознакомления с другими опциями `Crusher` - см. `Приложение А`.

## 4. Мониторинг/управление фаззингом

Осуществляется с помощью пользовательского интерфейса (UI).

1. Запуск UI (во 2-м терминале):
```shell
$ sudo -E <crusher_dir>/bin_x86-64/ui -o out
```

Основные показатели - см. блок `Stats` (правый верхний):

- время запуска - `run time`;
- число запусков - `execs done`;
- число образцов вх. данных (инпутов), приводящих к росту покрытия - `total_paths`;
- число инпутов, приводящих к аварийному завершению (крешу) - `unique_crashes`;
- число инпутов, приводящих к зависанию - `unique_hangs`;
- покрытие исходного кода - `Line Coverage`: число покрытых строк, общее число строк, процент покрытия;
- покрытие бинарного кода - `Block Coverage`: число покрытых базовых блоков (ББ), общее число ББ в бинаре, процент покрытия.

Более подробно про `UI` - см. раздел "Пользовательский интерфейс" в Руководстве пользователя Crusher (crusher/README.pdf).

2. Остановка фаззинга.
`Ctrl+C` в 1-м терминале или через `UI`.

## 5. Анализ результатов

Результаты фаззинга:

1. Наборы инпутов, приводящих к росту покрытия и следующим событиям:

1) Нормальное завершению программы - директория `out/EAT_OUT/queue`;
2) Аварийное завершению (креш) - директория `out/EAT_OUT/crashes`;
3) Зависание (работа программы больше отведённого ей таймаута) - директория `out/EAT_OUT/hangs`.

2. Набор отчётов.
Часть отчётов относится ко всему процессу фаззинга, часть - к конкретному инпуту и хранится в `out/EAT_OUT/results/{queue, crashes, hangs}/<input_id>/<analyzer_name>`.
Например, отчёт о покрытии бинарного кода для инпута `out/EAT_OUT/queue/id_queue_000000` будет сохранён в файл `out/EAT_OUT/results/queue/000000/lighthouse`.

Подробнее об отчётах можно узнать в разделах "Встроенные обработчики" и "Пользовательские обработчики" в Рук-ве пользователя.

### 5.1. Покрытие исходного кода

Краткая сводка по покрытым строкам выводится в UI. 
Полный отчёт в формате LCOV (HTML) хранится в директории `out/EAT_OUT/results/common_results/coverage/result_html/`.
Для просмотра в браузере:
```shell
$ sudo chown $USER:$USER -R out/
$ firefox out/EAT_OUT/results/common_results/coverage/result_html/index.html
```

### 5.2. Покрытие бинарного кода

Отчёт сохраняется в файл `out/EAT_OUT/results/lighthouse_total`.
Пример выдержки из отчёта:
```text
DRCOV VERSION: 2
DRCOV FLAVOR: drcov
Module Table: version 4, count 7
Columns: id, containing_id, start, end, entry, offset, path
0, 0, 0x0000000000000000, 0xffffffffffffffff, 0x0, 0x0, <memory>
1, 0, 0x00000040018f0000, 0xffffffffffffffff, 0x0, 0x0, /usr/lib/x86_64-linux-gnu/ld-linux-x86-64.so.2
2, 0, 0x0000004001a21000, 0xffffffffffffffff, 0x0, 0x0, /usr/lib/x86_64-linux-gnu/libc.so.6
3, 0, 0x0000004001a05000, 0xffffffffffffffff, 0x0, 0x0, /usr/lib/x86_64-linux-gnu/libz.so.1.2.11
4, 0, 0x000000400196e000, 0xffffffffffffffff, 0x0, 0x0, /usr/lib/x86_64-linux-gnu/libpcre2-8.so.0.10.4
5, 0, 0x0000004001932000, 0xffffffffffffffff, 0x0, 0x0, /usr/lib/x86_64-linux-gnu/libcrypt.so.1.1.0
6, 0, 0x0000004000000000, 0xffffffffffffffff, 0x0, 0x0, /root/fuzz/nginx-clean/sbin/nginx
BB Table: 8476 bbs
module id, start, size:
module[1]: 0x0000000000020290, 8
module[1]: 0x0000000000021030, 95
module[1]: 0x000000000002108f, 49
module[1]: 0x00000000000210da, 6
module[1]: 0x00000000000210c9, 17
...
```

Данный отчёт можно загрузить в ряд дизассемблеров с помощью плагина [lighthouse](https://github.com/gaasedelen/lighthouse).

